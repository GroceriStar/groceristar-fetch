const mealCalendar = require('./mealCalendar.js')

describe('testing static data files are set', () => {

  test('getMealPlan1', () => {
    var stream = mealCalendar.getMealPlan1()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan2', () => {
    var stream = mealCalendar.getWeekPlan2()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan3', () => {
    var stream = mealCalendar.getWeekPlan3()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan4', () => {
    var stream = mealCalendar.getWeekPlan4()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan5', () => {
    var stream = mealCalendar.getWeekPlan5()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan6', () => {
    var stream = mealCalendar.getWeekPlan6()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan7', () => {
    var stream = mealCalendar.getWeekPlan7()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan8', () => {
    var stream = mealCalendar.getWeekPlan8()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan9', () => {
    var stream = mealCalendar.getWeekPlan9()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan10', () => {
    var stream = mealCalendar.getWeekPlan10()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan11', () => {
    var stream = mealCalendar.getWeekPlan11()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan12', () => {
    var stream = mealCalendar.getWeekPlan12()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan13', () => {
    var stream = mealCalendar.getWeekPlan13()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan14', () => {
    var stream = mealCalendar.getWeekPlan14()
    expect(stream).not.toBe('')
  })

  test('getWeekPlan15', () => {
    var stream = mealCalendar.getWeekPlan15()
    expect(stream).not.toBe('')
  })

  test('getPlanningWeek16', () => {
    var stream = mealCalendar.getPlanningWeek16()
    expect(stream).not.toBe('')
  })

  test('getPlanningWeek17', () => {
    var stream = mealCalendar.getPlanningWeek17()
    expect(stream).not.toBe('')
  })

  test('getPlanningWeek18', () => {
    var stream = mealCalendar.getPlanningWeek18()
    expect(stream).not.toBe('')
  })

  test('getPlanningWeek19', () => {
    var stream = mealCalendar.getPlanningWeek19()
    expect(stream).not.toBe('')
  })

  test('getPlanningWeek20', () => {
    var stream = mealCalendar.getPlanningWeek20()
    expect(stream).not.toBe('')
  })

})
